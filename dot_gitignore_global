# Global gitignore (core.excludesFile). Personal/OS/editor cruft + a secrets
# backstop only — project artifacts (node_modules, dist, …) belong in each
# repo's committed .gitignore, not here.

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes
Icon?

# Editor (personal)
.idea/
*.swp
*.swo
*~
.netrwhist

# Secrets backstop (projects should also ignore these)
.env
.env.local
.env.*.local
*.pem
*.key
.secrets

**/.claude/settings.local.json
